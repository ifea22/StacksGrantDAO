
{
  "name": "stacks-grant-dao",
  "version": "1.0.0",
  "description": "Stacks Grant DAO - Decentralized funding platform for Stacks ecosystem projects",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "check": "clarinet check",
    "deploy": "clarinet deployments generate --testnet && clarinet deployments apply --testnet",
    "build:frontend": "cd frontend && npm run build",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd frontend && npm install",
    "start": "npm run check && npm run deploy && npm run build:frontend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hirosystems/clarinet-sdk": "^2.3.2",
    "@stacks/transactions": "^6.12.0",
    "chokidar-cli": "^3.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vitest": "^1.3.1",
    "vitest-environment-clarinet": "^2.0.0"
  }
}
